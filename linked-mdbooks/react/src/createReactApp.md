# Create React App

creating first single page react app

## prequesites
```bash
# node / npm / npx have to be installed
node --version
npm --version
npx --version
```

## create skeleton app
```bash
# create app 
npx create-react-app <appname>
```








