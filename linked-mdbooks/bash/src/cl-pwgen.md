# pwgen

```bash
# install
apt-get install -y pwgen

# generate 160 8-char random pw (memorable)
pwgen 

# generate 180 8-char random pw (SECURE)
pwgen -s 

# generate 14-char lenght passwords!
pwgen -s 14 

# generate 14-char length passwords (2 times)
pwgen -s 14 2
```